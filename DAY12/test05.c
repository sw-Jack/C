#include <stdio.h>

int main(void) {
	// 1. 3개의 배열 요소를 가진 정수형 배열 kim을 선언하고 각 요소에 1, 2, 3을 대입하여 초기화하고 출력하시오
	/*int kim[3];
	kim[0] = 1;
	kim[1] = 2;
	kim[2] = 3;
	for (int i = 0; i < 3; i++) {
		printf("arr[%d] = %d\n", i, kim[i]);
	}*/




	// 2. 길이가 3인 Float형 배열을 두 개 선언하고 한 개는 선언과 동시에 초기화를 진행하고 한 개는 선언 후 각 요소에 접근하여 값을 넣어 초기화
	// 진행 후 두 배열의 각 요소를 소수점 둘째자리까지만 출력하시오
	//float arr1[3] = { 10, 20, 30 };
	//float arr2[3];
	//arr2[0] = 40;
	//arr2[1] = 50;
	//arr2[2] = 60;

	//for (int i = 0; i < 3; i++) {
	//	printf("arr1[%d] = %.2f\n", i, arr1[i]);
	//}
	//for (int i = 0; i < 3; i++) {
	//	printf("arr2[%d] = %.2f\n", i, arr2[i]);
	//}





	// 3. 문자 10개를 저장할 수 있는 배열을 만들고 10개의 문자를 입력 받아 입력 받은 문자 중에 3번째와 7번째 문자를 출력하시오
	//char arr[10] = { 'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j' };
	//for (int i = 0; i < 10; i++) {
	//	if (i == 2 || i == 6) {
	//		printf("arr[%d] = %c\n", i, arr[i]);
	//	}
	//}






	// 4. int 형 배열에 4개의 숫자를 입력 받아 각 요소의 합을 구하는 프로그램을 만드시오
	//int arr[4] = { 10, 90, 100, 30 };
	//int sum = 0;
	//for (int i = 0; i < sizeof(arr)/sizeof(int); i++) {
	//	sum += arr[i];
	//}
	//printf("배열 원소 총 합 : %d\n", sum);





}