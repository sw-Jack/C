#include <stdio.h> 

int main(void) {

	// do ~ while
	// - 무조건 한번은 실행해야하며 잘못된 데이터를 걸러낼 때 사용한다.
	// - 형식의 특징 상 종속 문장을 먼저 실행하고 그 후에 조건식을 비교하는 형태
	// - 반복문 안에서 만들어진 결과물을 조건식에서 비교하기에 좋다.
	// while과 구분 짓기 위해 제일 마지막에 세미콜론(;)을 붙인다.

	// - 형식
	// do {
	//		종속 문장;
	// } while( 조건식 );
	//			
	return 0;
}