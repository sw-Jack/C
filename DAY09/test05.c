#include <stdio.h>

// 전역 변수
// - 어느 한 지역에 속하지 않고 프로그램 전역적으로 사용하기 위한 변수
// - 프로그램이 실행될 때 공간이 생성되고 프로그램이 끝날 때 공간이 소멸되는 변수
// - 초기화를 하지 않으면 자동으로 NULL 값 초기화된다
int main(void) {

	// 지역 변수
	// - 어느 한 지역 또는 영역에 속해서 그 지역 내에서 우선적으로 사용되는 변수
	// - 지역이 실행될 때 공간이 생성되고 그 지역이 끝날 때 공간이 소멸되는 변수

	return 0;
}